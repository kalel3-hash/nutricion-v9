"use client";

export default function AnalizarClient() {
  return (
    <div style={{ padding: 16 }}>
      <h1>Analizar</h1>
      <p>OK: autenticación validada en servidor (layout de (protected)).</p>
    </div>
  );
}